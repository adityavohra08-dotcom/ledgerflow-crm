[build]
buildCommand = "npm install && npm run web:build -w @ledgerflow/web"

[deploy]
startCommand = "npm run web:start -w @ledgerflow/web"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"